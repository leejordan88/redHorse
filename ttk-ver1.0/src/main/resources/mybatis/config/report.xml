<?xml version="1.0" encoding="UTF-8"?>
<!-- Sql Mapper -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="report">

<resultMap type="MemberPicVO" id="memberPicRM">
	<result column="id" property="memberVO.id"/>
</resultMap>

 <insert id="reportMemberPic" parameterType="memberPicReportVO">
 	<selectKey keyProperty="pictureReportNo" resultType="int" order="BEFORE">
 		select pictureReport_seq.nextval from dual
 	</selectKey>
 	insert into pictureReport(pictureReportNo, pictureNo, pictureReportDate, pictureReportState, pictureReportContent, reporter)
 	values(#{pictureReportNo}, #{pictureNo}, sysdate, #{pictureReportState}, #{pictureReportContent}, #{reporter})
 </insert>
</mapper>



