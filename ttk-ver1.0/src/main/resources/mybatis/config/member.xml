<?xml version="1.0" encoding="UTF-8"?>
<!-- Sql Mapper -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	<sql id="selectMember">
 		select id,password,name,tel,sex,age,address,introduce,profileimg,
 		range,enabled,hit from member
 	</sql>
<!--  <insert id="register" parameterType="memberVO">
 	insert into member (id, password, name,age, tel,sex,address,introduce) 
 	values(#{id}, #{password}, #{name},#{age},#{tel},#{sex}#{address},#{introduce})
 </insert> -->
 <select id="login" resultType="memberVO" parameterType="memberVO">
 <include refid="selectMember"></include>
 where id=#{id} and password=#{password}
 </select>
  	<update id="updateMember" parameterType="memberVO">
		update member set password=#{password},name=#{name},address=#{address}
		where id=#{id}
	</update>
</mapper>