<?xml version="1.0" encoding="UTF-8"?>
<!-- Sql Mapper -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberpic">

 <insert id="uploadMemberPic" parameterType="memberPicVO">
 	<selectKey keyProperty="pictureNo" resultType="int" order="BEFORE">
 		select memberPicture_seq.nextval from dual
 	</selectKey>
 	insert into memberPicture (pictureNo, id, filename, pictureTitle,pictureDate,pictureContent) 
 	values(#{pictureNo}, #{memberVO.id}, #{fileName},#{pictureTitle},sysdate,#{pictureContent})
 </insert>

</mapper>